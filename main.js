const quarterValue = (scaryNumber) => {
const quarter = scaryNumber / 4

return quarter
}

const returnedNumber = quarterValue(56)
console.log(returnedNumber)

// Invoke the function and store the return value


// Log the return value
console.log()